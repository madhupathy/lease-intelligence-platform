"""Q&A router placeholder (pgvector "ask this lease"). Skeleton only.

Note: first item in the AGENTS.md §10 cut order.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/qa", tags=["qa"])
