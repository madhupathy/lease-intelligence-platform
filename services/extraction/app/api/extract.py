"""Extraction router placeholder (upload + run pipeline). Skeleton only."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/extract", tags=["extract"])
